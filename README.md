# Homophonic Translation (HU → EN) — Reproducible Pipeline

This repository implements the modular pipeline: HU→IPA→search EN words (CMUdict)→EN TTS→HU ASR→scores.
Open `notebooks/01_quickstart.ipynb` after installing `requirements.txt`.
